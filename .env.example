# Environment variables for Todo Application Docker setup

# JWT Secret Key for authentication (generate a strong random key)
JWT_SECRET_KEY=your-super-secret-jwt-key-here-make-it-long-and-random

# OpenRouter API Key for AI chatbot integration
OPENROUTER_API_KEY=sk-your-openrouter-api-key-here

# Database configuration (these are set in docker-compose.yml but can be overridden)
POSTGRES_DB=todo_app
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# Frontend API URL (this is set in docker-compose.yml but can be overridden)
NEXT_PUBLIC_API_URL=http://localhost:7861/api